int f(int a, int b, int c) {
	return func( func(a,b), c);
}

-> int func (int a, int b);

-> Preciso guardar o int c ?
F:	 # Convenção de chamada: Guardando valores
	 addi $sp, $sp, -16
	 sw $ra, 0 ($sp)
	 sw $a0, 4 ($sp)
	 sw $a1, 8 ($sp)
	 sw $a2, 12 ($sp)
	 # Método:
	 jal func # func(a,b)
	 add $a0, $v0, $0
	 add $a1, $a2, $0
	 jal func # func ( func(a,b), c)
	 # Restauração:
	 lw $a2, 12 ($sp)
	 lw $a1, 8 ($sp)
	 lw $a0, 4 ($sp)
	 lw $ra, 0 ($sp)
	 addi $sp, $sp, 16
	 jr $ra
	 
